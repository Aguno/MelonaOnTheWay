<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kaist.aguno.melona">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name = ".GlobalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cute"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data android:name="com.kakao.sdk.AppKey"
            android:value="@string/kakao_app_key"/>
        android:name =
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".QuestList"/>

        <activity android:name=".MyPageDetail"/>

        <activity android:name=".Report"/>

        <activity android:name=".MyPageDetail2"/>

        <activity android:name=".KakaoSignupActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".AddQuest"/>
        <activity android:name=".QuestDetail"></activity>
        <activity android:name=".MyPage"></activity>
    </application>

</manifest>